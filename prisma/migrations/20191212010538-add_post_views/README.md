# Migration `20191212010538-add_post_views`

This migration has been generated by Ryan Dsouza at 12/12/2019, 1:05:38 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Post" (
  "author" TEXT    REFERENCES "User"(id) ON DELETE SET NULL,
  "content" TEXT    ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "published" BOOLEAN NOT NULL DEFAULT false  ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "views" INTEGER NOT NULL DEFAULT 0  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Post" ("author","content","createdAt","id","published","title","updatedAt") SELECT "author","content","createdAt","id","published","title","updatedAt" from "Post"

DROP TABLE "lift"."Post";

ALTER TABLE "lift"."new_Post" RENAME TO "Post";

CREATE UNIQUE INDEX "lift"."Post.id" ON "Post"("id")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191212010235-init..20191212010538-add_post_views
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
   default  = true
 }
 generator photon {
@@ -14,8 +14,9 @@
   updatedAt DateTime @updatedAt
   published Boolean
   title     String
   content   String?
+  views     Int      @default(0)
   author    User?
 }
 model User {
```

## Photon Usage

You can use a specific Photon built for this migration (20191212010538-add_post_views)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191212010538-add_post_views'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
